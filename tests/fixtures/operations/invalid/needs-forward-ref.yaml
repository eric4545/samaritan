name: Needs Forward Ref Test
version: 1.0.0
description: A step that depends on a LATER step (can never be satisfied)

environments:
  - name: staging
    description: Staging
    variables: {}

steps:
  - name: First
    type: manual
    needs: [Second]
    command: echo first
  - name: Second
    id: second
    type: manual
    command: echo second
